import React, { Component } from 'react'
import Logout from '../components/Logout'

const loginPage = () => {
    return (
        <div>
            <Logout />
        </div>
    )
}

export default loginPage
